# LLM 工程说明

## 1. 目标与边界

本项目是面向方言学习者的 Web 助手，支持粤语、台山话、闽南话、上海话、潮州话、温州话和四川话的对话、翻译、词典、练习与声调学习。

它不是语言学权威工具：模型生成的方言表达需要由学习者或母语者复核；本地词典命中也不等同于完整句子翻译质量。

## 2. Prompt 设计

前端在 `main/app.js` 中维护两类 Prompt：

- 对话系统提示：限定服务范围为方言学习，要求提供注音、标准中文解释和生活场景例句；有词典结果时优先引用词典。
- 翻译方向提示：按方言和双向翻译分别定义术语、注音和输出格式约束，例如粤语使用粤拼、闽南语使用台罗/白话字、上海话提示罗马化或 IPA。

设计取舍：翻译 Prompt 要求“只输出翻译结果”，以便页面直接展示；对话 Prompt 要求 Markdown，以便呈现解释、清单和表格。Prompt 只约束输出，不能保证事实正确，因此需要工具结果和安全检查配合。

## 3. 检索与词典

项目当前不是向量数据库 RAG。已实现的是**本地结构化词典检索增强**：

- 服务端 `server/mcp.js` 加载 CSV 词典，提供 `search_dialect_dictionary` 和 `translate_dialect`。
- 前端也加载公开词典 CSV，用于页面内的词条与学习体验。
- 工具结果会被送回模型上下文，Prompt 明确要求优先使用其中的注音和释义。

该选择降低了部署复杂度并使核心词典查询可复现，但召回方式是字段包含匹配，不能处理语义检索或长文本。向量检索、来源版本和人工标注质量集属于后续阶段。

## 4. Tool 与 Agent

MCP 风格接口提供词典、翻译、出题、声调、例句、方言对比、学习进度和记忆等工具：

- `/mcp/tools` 列出工具 schema；
- `/mcp/call` 执行工具；
- `/mcp` 支持 `tools/list` 和 `tools/call` 的 JSON-RPC 形式。

`collaborate_learning_task` 使用三个独立分支：学习协调器规划步骤、词典研究器查询本地资料、安全审查器执行最终否决。服务端以 `Promise.all` 汇聚结果；安全分支拒绝时不返回合并结果。这是受控的任务分工，而非无限自主 Agent。

## 5. Memory

记忆通过哈希后的会话/用户标识隔离，保存学习偏好、笔记等短文本；写入前会脱敏手机号、邮箱和身份证样式文本，并限制长度与条数。它存储在本地 `data/memory/`，适合本地学习进度，不适合作为跨设备或长期敏感资料存储。

## 6. Safety、隐私与故障策略

输入与输出均经过 `server/guardrails.js`：

1. 硬规则先拦截越狱、有害内容、敏感信息索取和凭据泄露。
2. 配置了审核模型时，AI 审核器做第二层判断；输出风险内容可尝试安全改写。
3. 审核模型不可用时，受审核请求采用 **fail-closed**：拒绝执行，避免把安全判断静默降级为放行。

日志不写入 API Key、密码或完整输入。服务请求日志仅包含时间、`request_id`、方法、路径、状态码、耗时和错误码；安全事件中的预览会先脱敏。

## 7. Fallback 与可观测性

- 浏览器端的展示能力有词典、音频和复制等降级逻辑；模型对话仍需用户配置有效模型服务。
- 服务端模型审核不能用本地规则替代，故模型不可用会明确报错而非生成伪造答案。
- `logs/run_log.jsonl` 记录请求元数据，`npm run metrics` 生成成功率与延迟汇总；`npm run eval` 生成可重复的接口评测证据。

## 8. 验证方法

```bash
npm install
$env:GUARDRAIL_AI_ENABLED="0"; npm start
# 新开终端
npm run eval
node eval/auth-api-test.mjs
npm run metrics
```

离线评测覆盖确定性 MCP 工具和硬安全规则；它不声明模型翻译质量。模型质量需要在配置真实模型后，用人工标注的方言样本另行评审。
